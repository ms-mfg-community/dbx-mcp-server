# Databricks Configuration (defaults — can be overridden per-request via HTTP headers)
DATABRICKS_HOST=https://adb-xxxxxxxxxxxxx.azuredatabricks.net
DATABRICKS_TOKEN=dapi123456789abcdef

# Warehouse Configuration (for SQL queries)
DATABRICKS_WAREHOUSE_ID=xxxxxxxxxxxxxxxx

# Database Configuration
DATABRICKS_CATALOG=dbx_1
DATABRICKS_SCHEMA=default

# MCP Server Configuration
# Transport: "stdio" for local dev (default), "streamable-http" for local HTTP server or Azure hosting
MCP_TRANSPORT=stdio
MCP_SERVER_PORT=8000
MCP_SERVER_HOST=0.0.0.0
MCP_LOG_LEVEL=INFO
